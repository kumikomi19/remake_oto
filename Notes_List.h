#pragma once
#include "DxLib.h"
#include "main.h"
#include "Block.h"

class Notes_List {
private:

public:
	

	void Notes(int count, int start, int notes, int hight, int Hit_position);
	void List_A(int count, int Hit_position);
};